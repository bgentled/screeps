var config = {
    mainSpawn                  : 'Mainframe',
    maxHarvesters              : 5,
    maxBuilder                 : 1,
    maxUpgrader                : 3,
    emergencyHarvesterBodyParts: [WORK, CARRY, CARRY, MOVE, MOVE]
};

module.exports = config;