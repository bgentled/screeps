var config = {
    mainSpawn                  : 'Mainframe',
    maxHarvesters              : 4,
    maxBuilder                 : 2,
    maxUpgrader                : 1,
    emergencyHarvesterBodyParts: [WORK, CARRY, CARRY, MOVE, MOVE]
};

module.exports = config;