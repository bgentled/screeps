var config = {
    mainSpawn                  : 'Mainframe',
    maxHarvesters              : 4,
    maxBuilder                 : 2,
    maxUpgrader                : 3,
    emergencyHarvesterBodyParts: [WORK, CARRY, CARRY, MOVE, MOVE]
};

module.exports = config;