var config = {
    mainSpawn                  : 'Mainframe',
    maxHarvesters              : 4,
    maxBuilder                 : 1,
    maxUpgrader                : 4,
    emergencyHarvesterBodyParts: [WORK, CARRY, CARRY, MOVE, MOVE]
};

module.exports = config;